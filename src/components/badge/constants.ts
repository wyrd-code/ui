import { Colors, Positions } from '../../models/enums'

export const ALLOWED_POSITIONS = [
  Positions.TL,
  Positions.TR,
  Positions.BL,
  Positions.BR,
]

export const ALLOWED_TYPES = [
  Colors.NEUTRAL,
  Colors.PRIMARY,
  Colors.SECONDARY,
  Colors.ACCENT,
  Colors.INFO,
  Colors.SUCCESS,
  Colors.DANGER,
  Colors.WARNING,
  Colors.BLACK,
]
